select METADATA_ELEM.Item_ID,'',group_concat(replace(Content,'\n',' ') SEPARATOR ' *** ') from ARCHIVED_ITEM inner join METADATA_ELEM on ARCHIVED_ITEM.Item_ID = METADATA_ELEM.Item_ID where (TagName = 'publisher' or TagName = 'description' or TagName = 'creator' or TagName = 'title' or TagName = 'contributor' or TagName = 'subject') and Archive_ID = 472 group by METADATA_ELEM.Item_ID;
